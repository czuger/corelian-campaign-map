.h1
  Liste des joueurs de la campagne

%table.table
  %thead
    %tr
      %th
        Joueur
      %th{ colspan: 2 }
        Camp
      %th
        Admin

  %tbody
    - @players.each do |player|
      %tr
        %td= player.user.name
        %td= empire_button(player)
        %td= rebel_button(player)
        %td= player.admin ? 'Oui' : 'Non'
